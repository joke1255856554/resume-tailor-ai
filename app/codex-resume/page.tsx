import { redirect } from 'next/navigation'

/** Phase 5.2 POC has been merged into the product's main customization flow. */
export default function CodexResumeExperimentRedirect() {
  redirect('/')
}
